# Procedência — mãos de primeira pessoa

Este diretório ainda contém somente o asset legado `arms.glb`, que não atende ao
contrato de `plans/26-VIEWMODEL-MAOS-FACCIONAIS.md`: ele não possui juntas de dedos
nem clipes próprios. Portanto não é uma fonte aprovada para tornar mãos padrão.

Quando o candidato `arms-v2.glb` existir, registre abaixo, por variante, antes de
integrá-lo:

| arquivo | variante | fornecedor/modelo | ID da geração | plano e direito comercial | data | SHA-256 | revisão |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `arms-v2.glb` | pendente | pendente | pendente | pendente | pendente | pendente | pendente |

O recibo, prompt e imagens de inspeção ficam em `references/viewmodel-maos/`, que
é insumo de trabalho local e não é redistribuído pelo repositório.
